## Звіт до роботи №4
# Тема: Робота з ООП

## Мета роботи: Навчитись працювати з Класами та його основними конструкціями;

## Виконання роботи

### 1. Базовий клас
```python
class MyAnimals:
    pass

a = MyAnimals()
print(type(a))
```
**Результат:**  
```
<class '__main__.MyAnimals'>
```
### 2 Порівняння простих даних з об’єктами
```python
animals = ["Рибки", "Кіт", "Собака"]
name = ["Неони", "Мурка", "Рекс"]
age = [1, 5, 2]

class MyAnimals:
    def __init__(self, animals, name, age, mass=None) -> None:
        self.animals = animals
        self.name = name
        self.age = age
        self.mass = 0 if mass is None else mass

a1 = MyAnimals("Рибки", "Неони", 1)
a2 = MyAnimals("Кіт", "Мурка", 5, 10)
a3 = MyAnimals("Собака", "Рекс", 2)

a1.name = "Золоті"
a2.name = "Машка"
a3.name = "Барбос"
```
**Результат:** 
```python
Моя тваринка Золоті, вона важить 0 кг
Моя тваринка Машка, вона важить 10 кг
Моя тваринка Барбос, вона важить 0 кг
```

### 3 Методи класу
```python
class MyAnimals:
    def speak(self):
        ...
    def add_one_year(self):
        ...
    @classmethod
    def stats(cls, obj):
        return f"Тваринка {obj.animals}, ім’я {obj.name}, вік {obj.age} років"
```
**Результат:** 
```python
Тваринка Рибки, ім’я Неони, вік 1 років
Тваринка Кіт, ім’я Мурка, вік 5 років
Тваринка Собака, ім’я Рекс, вік 2 років
```

### 4. Змінні класу
```python
class MyAnimals:
    total_animals = 0
    total_animal_types = set()
```
Створено кілька об’єктів у циклі, глобальні змінні класу автоматично оновлюються.  
**Результат:**  
```
В мене зараз є тваринки 
['Рибки', 'Кіт', 'Собака'], їх кількість 5
```

---
### 5. Статичні методи та self
```python
@staticmethod
def call_pet():
    return "Тваринка біжить до нас"
```
Виклик статичного методу для кожного об’єкта `a1`, `a2`.  
**Результат:**  
```
Тваринка біжить до нас
Тваринка біжить до нас
```
---

### 6. Properties
```python
@property
def live_untill(self):
    return datetime.today().year - self.age + self.max_age

@property
def is_adult(self):
    return self.age >= 2
```
Додано властивості `live_untill` та `is_adult`.  
**Результат:**  
```
Мій Кіт буде зі мною до 2040 року
Чи моя тваринка доросла? True
```

---

### 7. Методи класу
```python
@classmethod
def from_csv(cls, csv_data:str):
    ...
@classmethod
def check_name_capitalized(cls, obj):
    return obj.name[0].isupper()
```
Метод `from_csv` створює об’єкт із CSV-рядка.  
Метод `check_name_capitalized` перевіряє, чи ім’я починається з великої букви.  
**Результат:**  
```
Ім’я Мурка з великої букви? True
Ім’я Рекс з великої букви? True
Ім’я неони з великої букви? False
```



## Висновки

**Що зроблено в роботі:**  
У роботі було реалізовано приклади використання класів у Python: змінні класу, статичні методи, властивості (properties), методи класу та базовий клас. Для кожного завдання написано код, наведено результати виконання та пояснення.

**Чи досягнуто мети роботи:**  
Так, мета роботи досягнута — отримано практичні навички роботи з класами та закріплено теоретичні знання.

**Які нові знання отримано:**  
- Як працюють змінні класу та чим вони відрізняються від атрибутів об’єкта.  
- Як викликати статичні методи та методи класу.  
- Як створювати properties для обчислюваних атрибутів.  
- Як перевіряти правильність даних за допомогою методів класу.  
- Як створювати базовий клас і визначати тип об’єкта.

**Чи вдалось відповісти на всі питання задані в ході роботи:**  
Так, усі питання були розглянуті.

**Чи вдалося виконати всі завдання:**  
Так, усі завдання виконані.

**Чи виникли складності у виконанні завдання:**  
Невеликі складності виникали з правильним використанням змінних класу через об’єктиа також із розрізненням викликів методів через об’єкт та через клас. Всі труднощі були успішно подолані.

**Чи подобається такий формат здачі роботи (Feedback):**  
Так, формат README є зручним і зрозумілим. Він дозволяє швидко перевірити роботу, побачити код та результати його виконання.